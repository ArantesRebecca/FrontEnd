import React from 'react';
import PropTypes from 'prop-types';
import { TextField } from './styles';

const SearchBar = () => (
    <TextField placeholder={"Search"} variant="outlined"></TextField>
);

export default SearchBar;