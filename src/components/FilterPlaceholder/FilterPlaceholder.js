import React, { Fragment } from 'react';
import { Message, Wrapper } from './styles';

const FilterPlaceholder = () => (
  <Wrapper>
    <Message>Este filtro ainda não está disponível</Message>
  </Wrapper>
);

export default FilterPlaceholder;